# Dalye Platform

Monorepo Architektur

apps/
web → Cloudflare Pages frontend

packages/
ui → shared components
config → shared tooling

Stack
pnpm workspaces
turborepo
vite + react
cloudflare pages
idrive e2 assets

dev
pnpm install
pnpm dev

build
pnpm build
